# Install EKS

## Install using Fargate

```
eksctl create cluster --name demo-cluster --region sa-south-1 --fargate
```

## Delete the cluster after finishing

```
eksctl delete cluster --name demo-cluster --region sa-south-1
```
